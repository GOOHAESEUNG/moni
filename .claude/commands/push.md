# push

현재 브랜치를 원격에 푸시한다.

## 실행 순서

1. `git status`로 커밋되지 않은 변경사항이 있는지 확인한다
   - 미커밋 변경사항이 있으면 먼저 `/commit` 커맨드를 사용하라고 안내하고 중단한다
2. 현재 브랜치명을 확인한다
3. `git push -u origin {브랜치명}`으로 푸시한다
4. 완료 후 원격 브랜치 URL을 알려준다

## 규칙

- 미커밋 변경사항이 있으면 푸시하지 않는다
- main/master 브랜치에 직접 푸시하려는 경우 경고하고 확인을 받는다
